name: Test ARM runner labels

on:
  workflow_dispatch:

jobs:
  test-arm64:
    runs-on: ubuntu-24.04-arm64
    steps:
      - run: echo "ubuntu-24.04-arm64 works" && uname -m

  test-arm:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: echo "ubuntu-24.04-arm works" && uname -m
